item do
  expected do
    url_xpath
    url 'http://laurikari.net/tre/download/'
    xpath '//*[@id="post-5"]/div[2]/table/tbody/tr[1]/td[2]/a/tt'
    regex /^tre-([\d.]+).tar.bz2$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
